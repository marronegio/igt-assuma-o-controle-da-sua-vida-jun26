COLOQUE AS IMAGENS NESTA PASTA COM ESTES NOMES EXATOS:

1) logo-comando.png   -> a logotipo "COMANDO" (fundo transparente)
2) hero-bg.jpg        -> o banner do Geronimo no palco (fundo da hero, formato largo)

A landing page (index.html) já aponta para esses arquivos.
Basta salvar as imagens aqui com esses nomes e abrir o index.html no navegador.

OBS: a antiga "geronimo.png" (foto recortada) NÃO é mais usada.
